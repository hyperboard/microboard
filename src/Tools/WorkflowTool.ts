import { BoardTool } from "./BoardTool";

/**
 * Placeholder tool class for metadata-launched workflows.
 * UI shells should generally render the workflow from overlay metadata rather
 * than activate this tool directly.
 */
export class WorkflowTool extends BoardTool {}
